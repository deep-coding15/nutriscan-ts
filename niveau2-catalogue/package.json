{
  "name": "nutriscan-ts",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/script.ts --bundle --outfile=dist/bundle.js",
    "check": "tsc --noEmit",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.5.3"
  },
  "private": true,
  "dependencies": {
    "express": "^5.2.1"
  }
}
